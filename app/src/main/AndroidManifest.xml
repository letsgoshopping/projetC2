<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.project.lgs">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".AllSuppliersClasses.AllSuppliersAdapter" />
        <activity android:name=".AllSuppliersClasses.AllSuppliers" />
        <activity android:name=".AllProductsClasses.AllProducts" />
        <activity android:name=".SupplierClasses.SupplierDetails" />
        <activity android:name=".ProductDetails" />
        <activity android:name=".add_product" />
        <activity android:name=".ProblemActivity" />
        <activity android:name=".MainActivity"></activity>

        <activity
            android:name=".ProductClasses.ProductFragment"
            android:label="Products" />

        <activity
            android:name=".SearchClasses.NoResults"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".SearchClasses.SearchActivity"
            android:parentActivityName=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity
            android:name=".CartClasses.UserProfile"
            android:parentActivityName=".MainActivity">

                <intent-filter>
                    <action android:name="android.intent.action.SENDTO" />
                    <data android:scheme="mailto" />
                    <category android:name="android.intent.category.DEFAULT" />
                </intent-filter>
        </activity>

        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>